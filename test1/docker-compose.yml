version: "3.3"

services:
  postgres:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      - DATABASE_HOST='127.0.0.1'
      - POSTGRES_HOST_AUTH_METHOD=trust
      - POSTGRES_DB='hitscounter'
